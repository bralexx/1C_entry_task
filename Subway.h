#include <vector>

#define INF 1e9

struct Edge {
    int from;
    int to;
    double time;
};

struct Node {
    std::vector<time> edges
};

class Subway {
public:
    Subway(int n, const std::vector<Edge>& edges, const std::vector<int>& lines) {

    }
private:
    std::vector<Nodes>
};